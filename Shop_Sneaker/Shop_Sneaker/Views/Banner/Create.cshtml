@model CreateBanner
@{
    ViewBag.Title = "Create new Banner";
    var categories = ViewBag.Categories as List<Category>;
}

<form enctype="multipart/form-data" asp-controller="Banner" asp-action="Create" method="post">
    <div asp-validation-summary="All" class="text-danger"></div>
    @*<input type="hidden" asp-for="CreateBy" value="@CurrentAccount.Id" />*@
    <div class="form-group row">
        <label class="col-sm-2 col-form-label">Tên danh mục</label>
        <div class="col-sm-10">
            <input type="text" class="form-control" asp-for="BannerName">
        </div>
    </div>
    
    <div class="row form-group">
        <div class="col-sm-2">
            <label asp-for="BannerPhoto"></label>
        </div>
        <div class="col-sm-10">
            <div class="custom-file">
                <label class="custom-file-label" asp-for="BannerImage" for="customFile">Chọn ảnh</label>
                <input class="custom-file-input" value="nonCat.jpg" asp-for="BannerImage">
                <span asp-validation-for="BannerImage" class="text-danger"></span>
            </div>
        </div>
    </div>
    @*<div class="form-group row">
            <label class="col-sm-2 col-form-label">Người Tạo</label>
            <div class="col-sm-10">
                <input type="text" class="form-control" readonly value="@CurrentAccount.FullName">
            </div>
        </div>*@
    <button type="submit" class="btn btn-primary mb-2">Tạo Mới</button>
</form>

<script>
    function ClickCreatte() {
        alert("Tao Moi Thanh cong");
    }
</script>

@section Scripts{
    <script>
        $(document).ready(function () {
            $(".custom-file-input").on("change", function () {
                var fileName = $(this).val().split("\\").pop();
                $(this).siblings(".custom-file-label").addClass("selected").html(fileName);
                $("#avatar").attr("src", window.URL.createObjectURL($(this)[0].files[0]));
            });
        });
    </script>
}


